SET(LCMS_LIBNAME lcms2 PARENT_SCOPE)
SET(LCMS_INCLUDE_DIRNAME ${CMAKE_CURRENT_SOURCE_DIR}/liblcms2/include PARENT_SCOPE) #
SET(OPJ_HAVE_LCMS2_H 1 PARENT_SCOPE)
SET(OPJ_HAVE_LIBLCMS2 1 PARENT_SCOPE)
  
#
INCLUDE_DIRECTORIES("${CMAKE_CURRENT_SOURCE_DIR}/liblcms2/include")
#
FILE(GLOB SRCS ${CMAKE_CURRENT_SOURCE_DIR}/liblcms2/src/*.c)
FILE(GLOB HDRS ${CMAKE_CURRENT_SOURCE_DIR}/liblcms2/include/*.h)
#
SET(LIBTARGET "lcms2")
#
ADD_LIBRARY(${LIBTARGET} STATIC ${SRCS} ${HDRS})
#
IF(MSVC)
  SET_TARGET_PROPERTIES(${LIBTARGET} PROPERTIES PREFIX "lib")
ENDIF(MSVC)
#
SET_TARGET_PROPERTIES(${LIBTARGET}
  PROPERTIES
  OUTPUT_NAME "${LIBTARGET}"
  ARCHIVE_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/thirdparty/lib)
#
